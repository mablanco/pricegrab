package com.mablanco.pricegrab

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Placeholder unit test so the unit-test task is never empty. It will be
 * replaced (or accompanied) by the PriceComparator test suite when feature
 * 001 Phase 2 lands. Delete when at least one real test class exists in
 * this module.
 */
class ScaffoldSmokeTest {
    @Test
    fun scaffoldIsAlive() {
        assertTrue("scaffold smoke test", true)
    }
}

# Compose, AndroidX lifecycle and Kotlin reflection all ship their own
# consumer-proguard rules, so this file is intentionally minimal.
# Add keep rules here only when R8 removes something we actually need.
